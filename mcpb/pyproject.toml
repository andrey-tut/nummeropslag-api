[project]
name = "nummeropslag-mcp"
version = "1.0.1"
description = "Privacy-first Danish phone-number lookup MCP server"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27,<1",
    "mcp[cli]>=1.28,<2",
]
